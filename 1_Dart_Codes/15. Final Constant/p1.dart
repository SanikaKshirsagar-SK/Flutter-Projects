//const keyword
void main() {
  
  //const int? x;     // constant variable must be initialized

  const int? x = 10;
  print(x);

  // x = 50;        // cannot reassign value to constant variable or it cannot be updated
  // print(x);
}
